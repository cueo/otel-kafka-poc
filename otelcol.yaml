receivers:
  vmstats:
    collection_interval: 10s
    delay: 2
    count: 2

exporters:
  prometheus:
    endpoint: 0.0.0.0:8889

service:
  pipelines:
    metrics:
      receivers: vmstats
      exporters: prometheu
